<div class="container mt-5">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <h3 class="fw-bold">My Loan Offers</h3>
    <%= link_to "Post New Loan", new_loan_path, class: "btn btn-primary rounded-pill" %>
  </div>

  <% if @loans.any? %>
    <div class="row g-4">
      <% @loans.each do |loan| %>
        <div class="col-md-6 col-lg-4">
          <div class="card h-100 shadow-sm border-0 rounded-4">
            <div class="card-body">
              <h5 class="fw-semibold"><%= loan.title %></h5>
              <p class="text-muted small"><%= truncate(loan.description, length: 90) %></p>

              <ul class="list-unstyled mb-3">
                <li>💰 <strong>₹<%= loan.max_amount %></strong></li>
                <li>📈 <%= loan.interest_rate %>% Interest</li>
                <li>⏳ <%= loan.tenure_months %> Months</li>
              </ul>

              <div class="d-flex justify-content-between">
                <%= link_to "Delete", loan_path(loan),
                    method: :delete,
                    data: { confirm: "Are you sure?" },
                    class: "btn btn-outline-danger btn-sm rounded-pill" %>
              </div>
              
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="alert alert-info text-center">
      You haven’t posted any loans yet.
    </div>
  <% end %>
</div>
